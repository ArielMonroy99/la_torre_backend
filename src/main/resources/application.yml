spring:
   application:
      name:
         backend
   datasource:
      url:
         jdbc:postgresql://localhost:5432/torre
      driver-class-name:
         org.postgresql.Driver
      username:
         postgres
      password:
         password
   data:
      jdbc:
         dialect: postgresql
   jpa:
      hibernate:
         ddl-auto: create


server:
   port: 8080